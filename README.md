### experimental language (WIP)
`um` datatypes: vector, dequeue, strings

`rebuild` is a build system in plain shell

### clean and build
```
./rebuild clean build run
```

### plan
- macros
- type inference
